

<style>
main {
  background-image: url('<%= @team.image_url %>');
  background-size: cover;

  color: white;
}
</style>
<div>
<h1><%= @team.city %> <%= @team.name %> </h1>
</div>
<% @team.comments.each do |comment| %>
<p>
  <%= comment.content %><%= comment.address %>
</p>
<% end %>

<div>
  <h3>Top Pictures from around the <%= @team.name %> at the <%= @team.stadium %> Arena<h3>
    <ul>
      <% lat = @team.latitude %>
      <% long = @team.longitude %>
      <% Insta.location_tag(@team.instagram_id).each do |instagram| %>
      <li> <% insta_img = instagram.images["low_resolution"]["url"] %>
        <p>
          <img style="max-width:100px; max-height:100px;" src = "<%= instagram.images["low_resolution"]["url"] %>">
        </p>
      </li>
      <% end %>
    </ul>
  </section>
</div>
<div>
  <%= form_for ([@team, @team.comments.build]) do |f| %>
  <%= f.label 'comments' %><br>
  <%= f.text_area :content %><br>
  <%= f.text_area :address %><br>
  <%= f.text_area :city %><br>
  <%= f.submit %>
  <% end %>
</div>
